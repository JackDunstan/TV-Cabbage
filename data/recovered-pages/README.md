# Recovered Wayback pages

Raw HTML is stored under `raw/` using stable path-derived filenames. The first
pipeline test confirmed that Dynamic Views item captures can contain useful
title and Blogger post-ID metadata even when they omit the rendered post body.

Do not mark a page as recovered until its readable body is found or it is
verified as an alias of an existing local post.
